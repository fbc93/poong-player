mixin chartWithRemove(video, isLiked, index)
  li
    .rank-value=String(index+1).padStart(2,"0")
    .thumbnail(style=`background-image: url(${video.thumbUrl})`)
    .info
      .title=video.title
      if video.category === "풍월량"
        .category.poong=video.category
      if video.category === "쉬는시간"
        .category.rest-time=video.category
      if video.category === "기타영상"
        .category.etc=video.category
    i.view-icon 👀
    .view-value=video.views
    i(data-id=video._id).fa-solid.fa-play.play
    if isLiked 
      i.fa-solid.fa-thumbs-up.like
    else 
      i.fa-regular.fa-thumbs-up.like
    i.fa-solid.fa-trash-can.remove